# Logs

This folder stores application log files generated at runtime.

It is intentionally included in the repository so Git can track the directory, even when it is empty.

Log files are created automatically when the application runs and may be deleted automatically based on the application's log retention policy.
